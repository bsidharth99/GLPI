apiVersion: v1
kind: Service
metadata:
  name: glpi
  namespace: {{ .Release.Namespace }}
  labels:
    app: glpi
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: glpi
